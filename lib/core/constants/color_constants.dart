import 'package:flutter/material.dart';

class ColorConstants {
  static const Color primary = Color(0xFF6942FF);
  static const Color secondary = Color(0xFF1A2B59);
  static const Color background = Colors.black;
  static const Color deepPurple = Color(0xFF9A2DFF);
  static Color? darkGrey = Colors.grey[900];
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.grey;

  // Button Colors
  static const Color buttonBackground = primary;
  static const Color buttonText = textPrimary;

  // Terms Text Colors
  static const Color termsText = textSecondary;
  static const Color termsLink = primary;
}
