import 'package:flutter/material.dart';

class PaddingConstants {
  const PaddingConstants._();

  // Page Paddings
  static const EdgeInsets pagePadding = EdgeInsets.symmetric(horizontal: 24.0);
  static const EdgeInsets pageVerticalPadding = EdgeInsets.symmetric(vertical: 4.0);
  static const EdgeInsets pageHorizontalPadding = EdgeInsets.symmetric(horizontal: 24.0);

  // Dialog Paddings
  static const EdgeInsets dialogPadding = EdgeInsets.all(24.0);

  // Section Paddings
  static const EdgeInsets sectionPadding = EdgeInsets.all(16.0);
  static const double sectionSpacing = 12.0;
  static const double smallSpacing = 8.0;

  // Button Paddings
  static const EdgeInsets buttonPadding = EdgeInsets.symmetric(vertical: 16.0);

  // Content Paddings
  static const EdgeInsets contentPadding = EdgeInsets.all(16.0);
  static const double contentSpacing = 24.0;
}
